{
  "$id": "orderWizardSchematic",
  "title": "Material Stepper Order Wizard",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the component."
    },
    "spec": {
      "type": "string",
      "description": "Specifies if a spec file is generate.",
      "default": "true"
    }
  },
  "required": ["name"]
}
